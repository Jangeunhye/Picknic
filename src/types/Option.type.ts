export type Option = {
  title: string;
  labels: {
    firstInput: string;
    secondInput: string;
    thirdInput: string;
  };
};

export type Food = {
  title: string;
  spicy: boolean;
  oily: boolean;
  soup: boolean;
  rice: boolean;
};
